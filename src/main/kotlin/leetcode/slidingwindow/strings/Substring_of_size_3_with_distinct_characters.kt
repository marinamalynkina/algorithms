package leetcode.slidingwindow.strings

class Substring_of_size_3_with_distinct_characters {
//    fun countGoodSubstrings(s: String): Int {
//        s.lastIndex
//        s.substring(0,3)
//    }

    fun minimumDifference(nums: IntArray, k: Int): Int {
        nums.sort()
        return 1;
    }
}